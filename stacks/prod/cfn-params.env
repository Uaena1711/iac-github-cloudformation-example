# Sensitive CloudFormation parameters (NoEcho). Resolved from a GitHub secret, masked, merged
# into the change set in memory — never written to disk.
SECRETS_PROVIDER=github
NoticeValue=${DEMO_SECRET_PROD}
